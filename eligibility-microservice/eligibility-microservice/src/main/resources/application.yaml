server:
  port: 8085
spring:
  data:
    r2dbc:
      repositories:
        enabled: true
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/digital-document-delivery-system
    username: postgres
    password: $eQuE$aDa4002s
  cloud:
    function:
      definition: packageCreatedBinding
      scan:
        enabled: false
    stream:
      default-binder: kafka_default
      bindings:
        packageCreatedBinding-in-0:
          destination: event.package-created
          binder: kafka_default
          useNativeDecoding: true
        packageCreatedBinding-out-0:
          destination: event.package-eligible
          binder: kafka_default
          useNativeDecoding: true
      binders:
        kafka_default:
          type: kafka
          environment:
            spring:
              cloud:
                stream:
                  kafka:
                    default:
                      consumer:
                        configuration:
                          session.timeout.ms: 10000
                          max.poll.records: 10
                      producer:
                        compressionType: gzip
                        configuration:
                          max.request.size: 10000
                    binder:
                      autoAddPartitions: true
                      autoCreateTopics: true
                      requiredAcks: -1
                      minPartitionCount: 3
                      replicationFactor: 1
                      serdeError: logAndContinue
                      brokers: localhost:9092

